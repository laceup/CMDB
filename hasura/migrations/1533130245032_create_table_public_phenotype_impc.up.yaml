- args:
    sql: CREATE TABLE public."phenotype_impc"("mgi_accession_id" text NOT NULL, "marker_symbol"
      text NOT NULL, "human_gene_symbol" text NOT NULL, "marker_synonym" text NOT
      NULL, "marker_name" text NOT NULL, "mp_id" text NOT NULL, "no" serial NOT NULL,
      PRIMARY KEY ("no") )
  type: run_sql
- args:
    name: phenotype_impc
    schema: public
  type: add_existing_table_or_view
