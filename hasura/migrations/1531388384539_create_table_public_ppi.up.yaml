- args:
    sql: CREATE TABLE public."ppi"("interactor_a" text NOT NULL, "a_extra" text ,
      "interactor_b" text NOT NULL, "b_extra" text , "interaction_detection_method"
      text NOT NULL, "idm_definiton" text NOT NULL, "publication_1st_author" text
      NOT NULL, "publication_identifier" text NOT NULL, "id_ppi" serial NOT NULL,
      PRIMARY KEY ("id_ppi") )
  type: run_sql
- args:
    name: ppi
    schema: public
  type: add_existing_table_or_view
