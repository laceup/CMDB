- args:
    sql: CREATE TABLE public."ppi"("interactor_a" text NOT NULL, "a_extra" text ,
      "interactor_b" text NOT NULL, "b_extra" text , "interaction_detection_method"
      text NOT NULL, "idm_definition" text NOT NULL, "publication_1st_author" text
      NOT NULL, "publication_id" text NOT NULL, "ppi_id" serial NOT NULL, PRIMARY
      KEY ("ppi_id") )
  type: run_sql
- args:
    name: ppi
    schema: public
  type: add_existing_table_or_view
