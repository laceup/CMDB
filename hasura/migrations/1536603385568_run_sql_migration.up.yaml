- args:
    cascade: false
    sql: |-
      CREATE TABLE compartment(
      ensembl_id text,
      gene_name text,
      go_id text,
      location text,
      source text,
      acquired text,
      score integer
      )
  type: run_sql
